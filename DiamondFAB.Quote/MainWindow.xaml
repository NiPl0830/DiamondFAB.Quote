<Window x:Class="DiamondFAB.Quote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiamondFAB.Quote.ViewModels"
        mc:Ignorable="d"
        Title="DiamondFAB Quote" Height="500" Width="800" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <DockPanel Margin="10">
        <!-- ⚙️ Settings Button -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right">
            <Button Content="⚙️ Settings" Width="100" Height="30" Margin="0,0,0,10"
                    Click="SettingsButton_Click"/>
        </StackPanel>

        <!-- Main UI Content -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,20"/>

            <!-- Line Items Table -->
            <DataGrid Grid.Row="1" ItemsSource="{Binding LineItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="Auto"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Footer: Totals + Import/Export Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="5,0,0,0"/>
                    </Style>
                </StackPanel.Resources>
                <StackPanel>
                    <TextBlock Text="{Binding CurrentQuote.Subtotal, StringFormat='Subtotal: {0:C}'}" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentQuote.Tax, StringFormat='Tax: {0:C}'}" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentQuote.GrandTotal, StringFormat='Total: {0:C}'}" FontWeight="Bold" FontSize="16"/>
                </StackPanel>
                <Button Content="Import .PRT" Command="{Binding ImportPrtFileCommand}" Width="120" Height="40"/>
                <Button Content="Export PDF" Width="120" Height="40" Click="ExportPdfButton_Click"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>