<Window x:Class="DiamondFAB.Quote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiamondFAB.Quote.ViewModels"
        xmlns:conv="clr-namespace:DiamondFAB.Quote.Converters"


        mc:Ignorable="d"
        Title="{Binding AppTitle}"
        Height="640" Width="1180"
        MinHeight="560" MinWidth="980"
        WindowStartupLocation="CenterScreen"
        Background="#0F1115"
        AllowDrop="True"
        DragEnter="Window_DragEnter"
        Drop="Window_Drop">

    <!-- DataContext -->
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <!-- THEME + STYLES (matches SettingsWindow) -->
    <Window.Resources>
        <!-- Palette -->
        <SolidColorBrush x:Key="BgBrush"               Color="#0F1115"/>
        <SolidColorBrush x:Key="CardBrush"             Color="#181B22"/>
        <SolidColorBrush x:Key="CardBorderBrush"       Color="#242937"/>
        <SolidColorBrush x:Key="AccentBrush"           Color="#4C8DF6"/>
        <SolidColorBrush x:Key="AccentBrushHover"      Color="#6EA2FF"/>
        <SolidColorBrush x:Key="TextPrimaryBrush"      Color="#E9EDF5"/>
        <SolidColorBrush x:Key="TextSecondaryBrush"    Color="#AAB2C5"/>
        <SolidColorBrush x:Key="InputBgBrush"          Color="#111319"/>
        <SolidColorBrush x:Key="InputBorderBrush"      Color="#2A3040"/>
        <SolidColorBrush x:Key="InputBorderHoverBrush" Color="#3B4358"/>
        <SolidColorBrush x:Key="DividerBrush"          Color="#232838"/>
        <SolidColorBrush x:Key="DangerBrush"           Color="#9B1B30"/>
        <SolidColorBrush x:Key="DangerHoverBrush"      Color="#B0213A"/>
        <conv:ZeroToCollapsedConverter x:Key="ZeroToCollapsedConverter" />

        <!-- Typography -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="H1" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="H2" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="Overline" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Card -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="18" Opacity="0.18"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buttons -->
        <Style TargetType="Button" x:Key="BaseButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="8,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.95"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.90"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
        </Style>
        <Style TargetType="Button" x:Key="DangerButton" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
        </Style>

        <!-- Inputs -->
        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="{StaticResource InputBgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource InputBorderHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid (dark) -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="34"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource DividerBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource DividerBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="#0AFFFFFF"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1F2A40"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border x:Name="RowBd" Background="{TemplateBinding Background}" CornerRadius="8" Margin="2">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <SelectiveScrollingGrid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </SelectiveScrollingGrid.RowDefinitions>
                                <DataGridCellsPresenter Grid.Column="1"/>
                                <DataGridDetailsPresenter Grid.Column="1" Grid.Row="1" Visibility="Collapsed"/>
                            </SelectiveScrollingGrid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RowBd" Property="Background" Value="#12FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RowBd" Property="Background" Value="#203B82FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- LAYOUT -->
    <Grid Background="{StaticResource BgBrush}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*"/>
            <ColumnDefinition Width="7.8*"/>
        </Grid.ColumnDefinitions>

        <!-- TOP BAR -->
        <Border Grid.ColumnSpan="2" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource H1}" Text="DiamondFAB"/>
                    <TextBlock Style="{StaticResource H2}" Text="QUOTE" Margin="8,4,0,0"/>
                    <TextBlock Style="{StaticResource Overline}" Text="'Quoting jobs faster than your Wi-Fi'" Margin="8,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource DangerButton}" Click="ClearButton_Click" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🧹" Margin="0,0,6,0"/>
                            <TextBlock Text="Clear"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource PrimaryButton}" Click="SettingsButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙" Margin="0,0,6,0"/>
                            <TextBlock Text="Settings"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- LEFT: LOGO CARD (tall again with RowSpan=2) -->
        <Border Grid.Row="1" Grid.Column="0" Grid.RowSpan="2" Style="{StaticResource Card}" Margin="0,0,12,0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="220">
                <Viewbox Width="90" Height="90" Margin="0,0,0,12">
                    <Canvas Width="100" Height="100">
                        <!-- Main red diamond -->
                        <Polygon Points="50,0 100,50 50,100 0,50">
                            <Polygon.Fill>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#990000" Offset="0"/>
                                    <GradientStop Color="#CC0000" Offset="0.5"/>
                                    <GradientStop Color="#660000" Offset="1"/>
                                </LinearGradientBrush>
                            </Polygon.Fill>
                        </Polygon>
                        <!-- Facet highlight -->
                        <Polygon Points="50,0 100,50 50,50 0,50" Opacity="0.15">
                            <Polygon.Fill>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="White" Offset="0"/>
                                    <GradientStop Color="#FFEEEE" Offset="1"/>
                                </LinearGradientBrush>
                            </Polygon.Fill>
                        </Polygon>
                        <!-- Inner shadow -->
                        <Polygon Points="50,50 100,50 50,100 0,50" Opacity="0.25">
                            <Polygon.Fill>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#000000" Offset="0"/>
                                    <GradientStop Color="#440000" Offset="1"/>
                                </LinearGradientBrush>
                            </Polygon.Fill>
                        </Polygon>
                    </Canvas>
                </Viewbox>

                <TextBlock Text="DiamondFAB" Style="{StaticResource H1}" HorizontalAlignment="Center"/>
                <TextBlock Text="QUOTE" Style="{StaticResource H2}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <!-- RIGHT: GRID CARD -->
        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding LineItems}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      ColumnHeaderHeight="34"
                      RowHeight="34"
                      AlternationCount="2">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="4*"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="1*"/>
                    <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="1.5*"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="1.5*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- FOOTER -->
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Totals -->
            <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="{Binding CurrentQuote.Subtotal, StringFormat=Subtotal: {0:C}}" 
               Foreground="{StaticResource TextSecondaryBrush}"/>

                    <!-- Show discount only if > 0 -->
                    <TextBlock Text="{Binding CurrentQuote.DiscountAmount, StringFormat=Discount: -{0:C}}"
               Foreground="{StaticResource TextSecondaryBrush}"
               Visibility="{Binding CurrentQuote.DiscountAmount, Converter={StaticResource ZeroToCollapsedConverter}}"/>

                    <TextBlock Text="{Binding CurrentQuote.Tax, StringFormat=Tax: {0:C}}"
               Foreground="{StaticResource TextSecondaryBrush}"/>

                    <TextBlock Text="{Binding CurrentQuote.GrandTotal, StringFormat=Total: {0:C}}"
               FontWeight="Bold" FontSize="16"
               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- Actions -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                <Button Style="{StaticResource PrimaryButton}" Command="{Binding ImportXmlFilesCommand}" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⬆" Margin="0,0,6,0"/>
                        <TextBlock Text="Import .XML"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource PrimaryButton}" Click="ExportPdfButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🧾" Margin="0,0,6,0"/>
                        <TextBlock Text="Export PDF"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>